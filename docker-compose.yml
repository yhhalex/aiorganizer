services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: aiorganizer
      POSTGRES_USER: aiorganizer
      POSTGRES_PASSWORD: aiorganizer
    ports:
      - "55432:5432"
    volumes:
      - pgvector-data:/var/lib/postgresql/data

volumes:
  pgvector-data:
